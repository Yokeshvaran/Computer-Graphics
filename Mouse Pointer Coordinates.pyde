def setup():
    #global leftViewport, rightViewport;
    size(300, 300, P3D)
    print("Monitor display resolution is ");
    print("width is ",displayWidth);
    print("height is ",displayHeight);
    print("Width of the current canvas is ",width);
    print("Height of the current canvas is ",height);

def draw():
    print(mouseX,mouseY);
